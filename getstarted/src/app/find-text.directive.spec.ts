import { FindTextDirective } from './find-text.directive';

describe('FindTextDirective', () => {
  it('should create an instance', () => {
    const directive = new FindTextDirective();
    expect(directive).toBeTruthy();
  });
});
