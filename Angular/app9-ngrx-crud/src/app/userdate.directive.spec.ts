import { UserdateDirective } from './userdate.directive';

describe('UserdateDirective', () => {
  it('should create an instance', () => {
    const directive = new UserdateDirective();
    expect(directive).toBeTruthy();
  });
});
